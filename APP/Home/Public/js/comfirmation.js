(function ($){

	/**
	 *@param type
	 */
	function infoProve(type, name ) {
		alert(type);
		switch(type) {
			case 'text':

			break;
			case 'password':

			break;
			default:

			break;
		}
	}
})(jQuery);